{{- $debug := .Values.waktusolat.debug | default "false" | toString | quote }}
{{- $location := .Values.waktusolat.location | default "wlp-0" | toString | quote }}
{{- $api := .Values.waktusolat.mastodon.api | toString | quote }}
{{- $bot := .Values.waktusolat.mastodon.bot | toString | quote }}
{{- $scheduler_timezone := .Values.waktusolat.scheduler.timezone | default "Asia/Kuala_Lumpur" | toString | quote }}
{{- $clean_db := .Values.waktusolat.scheduler.schedule.clean_db | default "0" | toString | quote }}
{{- $notify_solat_schedule := .Values.waktusolat.scheduler.schedule.notify_solat_schedule | default "5" | toString | quote }}
{{- $notify_solat_times := .Values.waktusolat.scheduler.schedule.notify_solat_times | default "1" | toString | quote }}
{{- $post_scheduler := .Values.waktusolat.scheduler.schedule.post_scheduler | default "1" | toString | quote }}
{{- $redis_service := printf "redis://%s-waktusolat-svc" .Release.Name | quote }}
{{- $domain := .Values.waktusolat.domain | default "localhost" | toString }}
{{- $apscheduler := .Values.waktusolat.scheduler.apscheduler }}
{{- $celery := .Values.waktusolat.scheduler.celery }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-waktusolat-cm
  labels:
    {{- include "waktusolat.labels" . | nindent 4 }}
data:
  DEBUG: {{ $debug }}
  LOCATION_CODE: {{ $location }}
  API_BASE_URL: {{ $api }}
  BOT_ID: {{ $bot }}
  {{- if $celery }}
  CELERY_BROKER: {{ $redis_service }}
  CELERY_BACKEND: {{ $redis_service }}
  {{- end }}
  {{- if or $apscheduler $celery }}
  SCHEDULER_TIMEZONE: {{ $scheduler_timezone }}
  {{- end }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-waktusolat-site-config
  labels:
    {{- include "waktusolat.labels" . | nindent 4 }}
data:
  site-config.conf: |-
    {{- include "waktusolat.site-config-conf" . | replace "DOMAIN" $domain | nindent 4 }}
{{- if $apscheduler }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-waktusolat-apscheduler-config
  labels:
    {{- include "waktusolat.labels" . | nindent 4 }}
data:
  entrypoint.sh: |-
    {{- include "waktusolat.apscheduler-entrypoint-sh" . | nindent 4 }}
{{- else if $celery }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-waktusolat-default-celery-config
  labels:
    {{- include "waktusolat.labels" . | nindent 4 }}
data:
  celeryd: |-
    {{- include "waktusolat.default-celeryd" . | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-waktusolat-initd-celery-config
  labels:
    {{- include "waktusolat.labels" . | nindent 4 }}
data:
  celerybeat: |-
    {{- include "waktusolat.initd-celerybeat" . | nindent 4 }}
  celeryd: |-
    {{- include "waktusolat.initd-celeryd" . | nindent 4 }}
{{- end }}
{{- if or $apscheduler $celery }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-waktusolat-scheduler-config
  labels:
    {{- include "waktusolat.labels" . | nindent 4 }}
data:
  {{- if $apscheduler }}
  apps.py: |-
    {{- include "waktusolat.apscheduler-apps-py" . | nindent 4 }}
  {{- else if $celery }}
  celery.py: |-
    {{- include "waktusolat.celery-py" . | replace "CLEAN_DB_HOURS" $clean_db | replace "SOLAT_SCHED_HOURS" $notify_solat_schedule | replace "SOLAT_NOTIF_MINUTES" $notify_solat_times | replace "POST_SCHEDULER_SECONDS" $post_scheduler | nindent 4 }}
  init.py: |-
    {{- include "waktusolat.celery-init-py" . | nindent 4 }}
  {{- end }}
  tasks.py: |-
    {{- if $apscheduler }}
      {{- include "waktusolat.apscheduler-tasks-py" . | replace "CLEAN_DB_HOURS" $clean_db | replace "SOLAT_SCHED_HOURS" $notify_solat_schedule | replace "SOLAT_NOTIF_MINUTES" $notify_solat_times | replace "POST_SCHEDULER_SECONDS" $post_scheduler | nindent 4 }}
    {{- else if $celery }}
      {{- include "waktusolat.celery-tasks-py" . | nindent 4 }}
    {{- end }}
{{- end }}
